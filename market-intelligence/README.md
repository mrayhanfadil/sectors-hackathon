# experiment/market-intelligence

Track candidate branch for **market-intelligence**. No project code yet.

Project code lands here once we:
1. Pick the track (see `../ideas.md`)
2. Pick the specific idea
3. Pick the stack
4. Run the Sectors onboarding + claim the team API credits (per `../submission-checklist.md`)

Until then: `main` is the source of truth for rules + briefs.
