// FROZEN CONTRACT — generated from a live _build_live_payload render; do not hand-edit.
// Every field is optional: the FE must render a pending state when a section is absent, never a substitute
// value. Deep leaves are typed loosely on purpose — bind to what you read, not to what you hope is there.

export type ReportPayload = {
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  meta?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  cover?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  financial_highlights?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  segments?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  segments_src?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  segments_note?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  kpis?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  kpis_src?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  kpis_note?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  thesis?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  valuation?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  financials?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  financials_note?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  risks?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  risks_note?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  peers?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  news?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  sentiment?: unknown
  /** may be absent — the FE must render a pending state · filled in: neither ticker */
  strategy?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  catalysts?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  catalysts_note?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  exhibits?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  gate_inputs?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  key_financials?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  financial_statements?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  sector_data?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  filings_digest?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  corporate_actions?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  ownership_mix?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  free_float?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  revenue_combo?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  ebitda_combo?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  netprofit_combo?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  dcf_deep_dive?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  cDcf?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  fill_meta?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  house_rules?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  industry_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  performance_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  valuation_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  peers_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  cashflow_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  statements_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  key_ratio_page?: unknown
  /** may be absent — the FE must render a pending state · filled in: AMMN */
  critic?: unknown
}
